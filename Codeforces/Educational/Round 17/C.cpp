#include <bits/stdc++.h>

using namespace std;
#define endl '\n'

int main(){
    string a, b;
    cin>>a>>b;
    int  pre[100005];
    memset(pre, -1, sizeof(pre));
    for (int i = 0; i < b.size(); i++)
        for(int j = 0; j < a.size(); j++)
            for(int k = )
    return 0;
}